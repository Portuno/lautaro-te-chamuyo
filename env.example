# Laubot Environment Variables Template
# Copy this file to .env and fill in your actual values

# API Configuration (REQUIRED for production)
VITE_API_BASE_URL=https://your-api-domain.com

# Google OAuth Configuration (Optional - for Calendar integration)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_GOOGLE_REDIRECT_URI=https://your-domain.com/auth/callback

# Development vs Production Notes:
# - In development, missing values will use defaults with warnings
# - In production, missing critical values will throw errors
# - Always use HTTPS URLs in production

# Security Guidelines:
# 1. Never commit .env file to version control
# 2. Use different values for development/staging/production
# 3. Keep API keys and secrets secure
# 4. Regularly rotate credentials 